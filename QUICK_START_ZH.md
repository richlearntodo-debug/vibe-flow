# 言灵 · Vibe Flow Remote 快速开始

适用于 `v1.1.0`。正常情况下首次配置约 5 分钟，之后只需：**按住录音键说话，讲完后松开**。

## 下载

优先下载并运行 [`VibeFlow-Setup.exe`](https://github.com/richlearntodo-debug/vibe-flow/releases/latest/download/VibeFlow-Setup.exe)。

免安装版请下载 `Vibe-Flow-Windows-x64.zip`，完整解压后打开 `VibeFlow.exe`。不要下载 GitHub 自动生成的 `Source code (zip)` 或 `Source code (tar.gz)`，它们不是 Windows 安装包。

## 加入用户社区

首次配置遇到问题时，可扫码加入 Vibe Flow 用户社区获取帮助。

<img src="docs/images/vibe-flow-community.png" alt="Vibe Flow 用户社区二维码" width="640">

## 首次五步

1. **选择转写工具**
   在向导中选择微信输入法、Typeless、Windows 语音输入、Voquill 或其他支持全局快捷键的工具。

2. **安装 VB-CABLE**
   从 [VB-Audio 官网](https://vb-audio.com/Cable/) 下载驱动 ZIP，完整解压，右键 `VBCABLE_Setup_x64.exe` 选择“以管理员身份运行”，点击 **Install Driver**，然后重启 Windows。回到言灵后，`CABLE Input` 和 `CABLE Output` 都应显示已检测到。

3. **连接 RC003**
   打开 Windows 蓝牙设置，配对 `MI RC` 或“小米蓝牙语音遥控器”。回到向导，按一下方向键唤醒遥控器，等待“已连接，可以使用”。

4. **核对快捷键**
   快捷键必须与转写工具内部设置一致。微信输入法推荐开启“语音智能整理”，并在言灵选择 `Ctrl + Win + Shift` + “AI 整理（推荐）”；Typeless 常见为 `Right Alt` + 单击切换。点击“测试启动与结束”确认工具能正常打开和关闭。

5. **完成真实听写**
   点击测试输入框，按住遥控器录音键；看到首页录音波纹后说“测试麦克风，一二三四五六，期待效果”，讲完后松开。听到清晰、短促的结束音后，等待绿色成功状态且文字直接出现在测试框，再点击“完成设置”。

   微信兼容模式仍可使用 `Ctrl + Win` + “按住说话”，但该模式不会启用 AI 整理，并且在真机对比中更容易遗漏句尾，因此不作为默认配置。

![首次使用向导](docs/images/00-first-run.png)

## 日常听写

1. 聚焦任意输入框。
2. 按住录音键开始，首页遥控器麦克风会出现紫色光效。
3. 保持按住并自然说话。
4. 讲完后松开，松开事件会立即结束并提交。
5. 等待青色整理状态变为绿色完成状态。

RC003 固件会在一次物理长按约 60 秒时主动结束按键和音频流，这是硬件上限，不是微信输入法的限制。需要更长内容时请按自然段分次输入。语音听写页保留“连续听写”实验选项，但必须短按启动后立即松开，不能以持续长按方式使用。

## 默认按键

| 按键 | 默认操作 |
| --- | --- |
| 录音 | 按住开始，松开结束 |
| 确认 | `Enter` |
| Home | `Win + D` 显示桌面 |
| TV | 打开任务切换器；左/右选择，确认进入 |
| 功能键 | 打开或切回 ChatGPT 客户端 |
| 方向键 | 短按导航；长按上增加音量、长按下减少音量 |

在“按键快捷方式”页可以修改确认、Home、TV 和功能键。可选项包含复制、粘贴、保存、全选、查找、快速打开文件、新建终端、运行/调试和客户端启动等；重复分配会显示黄色提醒。

独立返回键和音量 +/- 键在已验证 RC003 的 Windows 蓝牙链路中没有稳定事件，因此本版不配置它们。

## 出现问题

打开“连接与自检”，按异常项右侧的按钮修复，再点击“重新自检”。若仍未解决：

1. 点击“复制问题摘要”。
2. 在 GitHub Issue 中说明使用的转写工具和现象。
3. 粘贴摘要；不要主动上传录音。

普通诊断不包含录音或识别文字。完整安装、配置与故障排查见 [中文使用教程](docs/USER_GUIDE_ZH.md)。
