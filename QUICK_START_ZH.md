# 言灵 · Vibe Flow Remote v1.3 快速开始

日常操作固定为：**聚焦输入框，按住录音键说话，松开结束，检查文字后按确认键发送。** 录音继续使用 v1.2.1 已验证的 `v1.0.3` 内核，单段约 60 秒。

## 当前发布状态

v1.3 尚在 RC003 硬件验收阶段。正式发布前请继续使用 [v1.2.1 安装版](https://github.com/richlearntodo-debug/vibe-flow/releases/download/v1.2.1/VibeFlow-Setup.exe)。不要下载 GitHub 自动生成的源码 ZIP。[所有公开版本](docs/VERSION_ARCHIVE_ZH.md)。

## 五项首次设置

1. 确认 Windows 10/11、RC003 和按住说话方式。
2. 配对 `MI RC` / RC003，并按方向键验证真实按键事件。
3. 安装或检查 VB-CABLE，确认 `CABLE Input` 与 `CABLE Output`。
4. 选择微信输入法、Typeless、豆包输入法、Windows 语音输入或其他工具，并完成一次真实转译。
5. 选择开机启动、自动连接和托盘行为，查看最终汇总。

![首次设置](docs/images/00-setup-01-device.png)

## 微信输入法推荐配置

- 全局快捷键：`Ctrl + Win`。
- 触发方式：单击切换。
- 启动等待：`80 ms`。
- 自动使用遥控器麦克风：开启。

其他工具以客户端实际快捷键为准，两边必须完全一致。

## 开始听写

1. 单击目标文本框，确认插入光标可见。
2. 按住录音键并自然说话。
3. 松开，等待一次结束提示音和文字转译。
4. 检查文字，按中间确认键发送。

达到 RC003 约 60 秒物理边界时会结束，不会自动开启第二个麦克风。长内容请分段输入。

## 快捷键

| 按键 | 默认操作 |
| --- | --- |
| 录音 | 按住收音，松开结束 |
| 功能键 | 短按复制，长按粘贴 |
| 方向键 | 标准方向，可分别配置一个动作 |
| 确认 | `Enter` |
| Home | 显示桌面，可配置短按/长按 |
| TV | 打开任务视图，可配置单击动作 |

可选动作包括 APP、HTTPS 网页、编辑、系统、媒体、区域截图和自定义快捷键。开机、返回和独立音量键不提供映射。

![快捷键配置](docs/images/03-shortcuts.png)

选择本机 APP 后，言灵会依次尝试切换运行窗口、启动有效 EXE、使用 Windows 开始菜单入口。点击播放图标收到真实成功回执后，再按实体遥控器验证。

## 设置与排查

- 主题：白天、夜间、跟随 Windows。
- 配置：备份、导入、恢复上次。
- 自检：从第一项红色或橙色项目开始处理。
- 诊断：不包含录音或转译文字，并隐藏用户路径、设备身份、URL 和应用目标。

完整说明见 [v1.3 图文教程](docs/V1_3_USER_GUIDE_ZH.md)。
