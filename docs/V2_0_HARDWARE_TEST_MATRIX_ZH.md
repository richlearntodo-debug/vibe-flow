# V2.0 真机与系统测试清单

> **范围提示（2026-09-08 起）：** 本清单服务当前 Host 的稳定语音、快捷键、工作流和安装生命周期。Notes Deck/便签记录已经归档；缺少设备、目标 APP 或语音工具时不得写成通过。

状态含义：`已观察` 仅描述本机取得的真实证据；`未验证` 不等同失败，也不能用于正式发布声明。

| 项目 | 状态 | 当前证据 / 下一步 |
| --- | --- | --- |
| RC003 单次按住/松开录音 | 已观察，需对最终二进制复验 | 较早实测约 3.48 秒、232 个音频包、单 generation、180 ms drain；最终候选需重跑 |
| RC003 方向/Home/TV/功能键 | 未验证 | 对最终 Bridge 逐键记录 Raw Input 与执行回执 |
| 普通键盘 F5 隔离 | 未验证 | 无签名过滤器时存在 V1.5 既有限制 |
| 100 次录音循环 | 未验证 | 检查重复 generation、旧 stop、卡键和路由恢复 |
| 10 / 30 / 接近 60 秒音频 | 未验证 | 记录 audio_ms、packet_count、queue_drops 和真实尾音 |
| 微信输入法与另一语音工具 | 未验证 | 用户目视确认文字；程序不读取转写内容 |
| 蓝牙晚启动、断连、休眠/唤醒 | 未验证 | 验证自动恢复和旧 generation 释放 |
| Smart Focus 连续 50 次 | 未验证 | ChatGPT 或其他实际目标逐次确认无错误输入 |
| Windows 10 x64 / Windows 11 x64 | 未验证 | 使用干净环境安装、启动和卸载 |
| 100% DPI | 最终候选 UI 已观察 | 打包目录 Host 的六页、五任务向导、快捷键、语音和自检页面；白天/夜间模式均实际切换 |
| 125% / 150% / 200% DPI | 未验证 | 1366x768 与 1920x1080，浅色/深色/跟随系统 |
| 全新安装 / V1.5 升级 / 二次升级 / 卸载 | 未验证 | 必须在一次性 Windows 账户并取得系统修改授权 |
| VB-CABLE UAC、安装与重启恢复 | 未验证 | 不自动操作音频设备；需用户授权执行 |

正式稳定版要求上述 P0 硬件和生命周期项取得可审计证据。候选包生成不代表这些项目通过。
