# 言灵 · Vibe Flow Remote 快速开始

适用于 `v1.2.1`。日常操作固定为：**聚焦输入框，按住录音键说话，松开结束，检查文字后按确认键发送。**

## 下载

优先使用 [VibeFlow-Setup.exe](https://github.com/richlearntodo-debug/vibe-flow/releases/download/v1.2.1/VibeFlow-Setup.exe)。免安装版请完整解压 `Vibe-Flow-Windows-x64.zip` 后打开 `VibeFlow.exe`。不要下载 GitHub 自动生成的源码压缩包。[历史版本固定下载入口](docs/VERSION_ARCHIVE_ZH.md)。

## 用户社区

<img src="docs/images/vibe-flow-community.png" alt="Vibe Flow 用户社区二维码" width="640">

## 首次设置

1. 了解按住说话。
2. 打开并检查 Windows 蓝牙。
3. 配对 `MI RC` / RC003。
4. 按方向键验证按键链路。
5. 检查遥控器麦克风。
6. 安装 VB-CABLE，确认 `CABLE Input` 与 `CABLE Output` 都存在。
7. 选择微信输入法、Typeless、豆包输入法、Windows 语音输入或其他工具。
8. 在内置输入框完成一次真实转译。
9. 保持或配置四个方向键，可将任一方向设为区域截图。
10. 选择是否登录 Windows 后自动启动。
11. 查看汇总；异常项进入“一键自检”。

![首次设置](docs/images/00-setup-01-intro.png)

## 微信输入法推荐配置

- 全局快捷键：`Ctrl + Win`。
- 工具触发方式：单击切换。
- 言灵启动等待：`80 ms`。
- 自动使用遥控器麦克风：开启。

其他工具以客户端实际快捷键为准。两边快捷键必须完全一致。

## 开始听写

1. 单击目标文本框，确认插入光标可见。
2. 按住录音键；出现真实收音反馈后说话。
3. 松开录音键；结束提示音只播放一次。
4. 等待文字直接进入原文本框。
5. 检查后按中间确认键发送。

当前稳定模式单段最长约 `60 秒`，这是 RC003 的物理按键边界。到达边界后请开始下一段，不要等待软件自动续接。

## 按键

| 按键 | 默认操作 |
| --- | --- |
| 录音 | 按住收音，松开结束 |
| 功能键 | 短按复制，长按粘贴 |
| 方向键 | 标准方向；可分别配置一个动作或区域截图 |
| 确认 | `Enter` |
| Home | 显示桌面 |
| TV | 打开任务视图；方向选择，确认进入 |

开机、返回和独立音量键不提供映射。TV 任务视图打开后，方向键临时用于选择程序，不执行用户自定义动作。

![快捷键配置](docs/images/03-shortcuts.png)

截图配置：选择一个方向，直接点击“设为区域截图”；也可在“执行动作”中选择“系统 · 区域截图”。测试后保存，需要恢复时点击“恢复方向导航”。

主题切换：打开“设置”，点击“白天模式”或“夜间模式”即可立即切换，无需重启言灵。

## 出现问题

先打开“一键自检”，从第一项红色或橙色卡片开始处理。重点检查蓝牙连接、真实按键、真实音频、VB-CABLE、语音工具快捷键和最近会话是否只有一次开始/结束。

必须先单击目标文本框并看到插入光标。转译由所选语音工具直接写入当前焦点，言灵不会通过剪贴板补写；焦点错误时文字可能进入其他窗口。仍未解决时导出诊断并提交 GitHub Issue；普通诊断不包含录音或转译文字。

完整说明见 [中文使用教程](docs/USER_GUIDE_ZH.md)。
