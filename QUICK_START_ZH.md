# 言灵 Vibe Flow 快速开始

言灵 Vibe Flow 把小米 RC003 / MI RC 遥控器的麦克风实时连接到微信输入法。言灵不负责转写或修改文字，微信输入法继续完成语音识别和结构化整理。

## 首次使用

1. 在 Windows 蓝牙设置中配对 `MI RC` 或“小米蓝牙语音遥控器”。
2. 从 [VB-Audio 官方网站](https://vb-audio.com/Cable/) 安装 VB-CABLE。
3. 打开 `VibeFlow.exe`，跟随首次设置完成三个步骤，并选择是否登录 Windows 后自动运行。
4. 在声音设置或微信输入法中，把语音输入麦克风选择为 `CABLE Output`。
5. 聚焦任意文字输入框，按住遥控器录音键说话，松开后结束。

注意：言灵输出音频到播放端点 `CABLE Input`；微信输入法需要选择与之对应的录音端点 `CABLE Output`。

## 默认按键

| 按键 | 操作 |
| --- | --- |
| 录音 | 按住微信语音输入 |
| 确认 | Enter |
| Home | Win + D |
| TV | 打开任务切换器；左右选择，确认进入 |
| 功能键 | 单击 Ctrl + Shift + P，可在按键页修改 |
| 方向键 | 短按为原生方向键；长按上增加系统音量，长按下减少系统音量 |

“按键配置”页提供 Vibe Coding、文本编辑、代码导航与评审三套预设，也可以逐项自定义快捷键。

当前 RC003 真机日志中，独立返回键和音量 +/- 键没有产生 Windows Keyboard、Raw Input 或 Consumer HID 事件。言灵只展示已经通过真机验证的单击与长按操作，不提供不稳定的组合键；系统音量使用长按上/下。

## 出现问题

打开言灵的“连接与诊断”页面，先点击“全面检测”。也可以刷新运行记录或导出诊断。诊断文本不包含录音、蓝牙地址和完整设备路径。
