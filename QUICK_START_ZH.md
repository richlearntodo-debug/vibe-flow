# 言灵 · Vibe Flow Remote 快速开始

言灵把小米 RC003 / MI RC 遥控器的麦克风连接到微信输入法、Typeless、Windows 语音输入、Voquill 或其他听写工具。按住时音频只在本机短暂缓存，松开后由所选工具完成语音识别和整理。

## 首次使用

1. 推荐从 GitHub Releases 下载并运行 `VibeFlow-Setup.exe`；免安装版需完整解压 ZIP 后再打开 `VibeFlow.exe`。首次打开先选择日常使用的转写工具，并选择是否登录 Windows 后自动运行。
2. 按页面安装或检测 VB-CABLE，确认 `CABLE Input` 和 `CABLE Output` 都已就绪。它是当前语音链路唯一必须额外安装的本地驱动。
3. 在 Windows 蓝牙中配对 `MI RC` 或“小米蓝牙语音遥控器”，回到向导等待“已连接，可以使用”。
4. 核对转写工具快捷键和触发方式，点击“测试启动与结束”。
5. 点击向导测试输入框，按住遥控器录音键说完一句话再松开。绿色成功状态和轻提示音出现后即可完成设置。

升级已有安装时可以覆盖程序文件，但不要删除运行目录中的 `vibe-mic-config.json`。发布包只携带 `vibe-mic-config.default.json` 模板，不会覆盖现有设置。

注意：言灵输出音频到播放端点 `CABLE Input`；听写开始前会临时把 Windows 默认录音端点切换到对应的 `CABLE Output`，结束后自动恢复。关闭自动路由时，才需要在转写工具中手动选择 `CABLE Output`。

## 默认按键

| 按键 | 操作 |
| --- | --- |
| 录音 | 按住，通过所选工具听写 |
| 确认 | Enter |
| Home | Win + D |
| TV | 打开任务切换器；左右选择，确认进入 |
| 功能键 | 默认打开或切回 ChatGPT 客户端，可在按键页选择其他 Agent 或开发工具 |
| 方向键 | 短按为原生方向键；长按上增加系统音量，长按下减少系统音量 |

“按键配置”页提供 Vibe Coding、文本编辑、代码导航与评审三套预设，也可以逐项自定义快捷键；右侧遥控器示意会同步高亮当前按键。

当前 RC003 真机日志中，独立返回键和音量 +/- 键没有产生 Windows Keyboard、Raw Input 或 Consumer HID 事件。言灵只展示已经通过真机验证的单击与长按操作，不提供不稳定的组合键；系统音量使用长按上/下。

## 出现问题

打开言灵的“连接与自检”页面，先看七项检查结果。异常项会显示直达修复按钮；处理后点击“重新自检”。提交问题时点击“复制问题摘要”，可直接粘贴到 GitHub Issue。普通诊断不包含录音、识别文字、蓝牙地址和完整设备路径。
